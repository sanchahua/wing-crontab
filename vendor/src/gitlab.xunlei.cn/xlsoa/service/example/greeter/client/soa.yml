modules:
    xlsoa:
        environment:
            hosts:                             
            - service: "xlsoa.example.greeter" # 定义xlsoa.example.greeter的host信息
              addr: "127.0.0.1:59092"          # xlsoa.example.greeter的连接地址
              oauth: false                     # 访问xlsoa.example.greeter不进行OAuth鉴权，直接访问

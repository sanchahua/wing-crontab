modules:
    xlsoa:
        server:
            context:
                addr: "127.0.0.1:59092"  # Server监听地址
                oauth:
                    secure:
                        switch: "off"    # Server关闭OAuth鉴权校验，允许Client直接访问
